import WhereTo from '../../common/WhereTo';
import React from 'react';
import { LiftScreens } from '../LiftScreens';

const LiftWhereTo: React.FC = () => <WhereTo nextScreen={LiftScreens.WHERE_FROM} />;

export default LiftWhereTo;
