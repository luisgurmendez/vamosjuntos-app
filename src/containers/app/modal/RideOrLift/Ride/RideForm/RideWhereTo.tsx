import WhereTo from '../../common/WhereTo';
import React from 'react';
import { RideScreens } from '../RideScreens';

const RideWhereTo: React.FC = () => <WhereTo nextScreen={RideScreens.WHERE_FROM} />;

export default RideWhereTo;
